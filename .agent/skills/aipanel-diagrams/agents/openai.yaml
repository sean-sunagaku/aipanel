interface:
  display_name: "AIPanel Diagrams"
  short_description: "Sub-agent-first draw.io bundle updates for aipanel"
  default_prompt: "Use $aipanel-diagrams to have a sub-agent draft the full aipanel diagram-bundle spec, then render and validate the architecture, flow, class, and persistence/data-model diagrams."
